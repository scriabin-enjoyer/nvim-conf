<leader>:
        - ld    (definition)
        - lD    (declaration)
        - lt    (type definition)
        - lf    (file)
        - li    (implementation)
        - lr    (references)
        - ls    (signature help)
        - lh    (lsp hover docs)

        - lws   (workspace symbol)
        - lds   (document symbols)
        - lR    (rename)
        - lc    (code action)
